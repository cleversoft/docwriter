doctype html
html
    head
        meta(charset="utf-8")
    body
        p Hi #{user.first_name},
        p Please follow the link below to reset your password:
        p
            a(href="#{config.app.url + '/reset-password/' + hash}") Reset Password